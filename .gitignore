# ── ОС и редакторы ─────────────────────────────────────────────
.DS_Store
Thumbs.db
*.iml
.idea/
.vscode/
*.sublime-project
*.sublime-workspace

# ── Node / фронтенд ────────────────────────────────────────────
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.eslintcache
.vite/
public/hot
public/build/
*.map

# ── PHP / Composer / Laravel ───────────────────────────────────
vendor/
composer.phar
.phpunit.result.cache
phpunit.xml.cache

# окружение
.env
.env.*
!.env.example
.env.backup

# storage и логи (каталоги будут существовать благодаря своим .gitignore)
storage/*.key
storage/debugbar/
storage/framework/
storage/logs/

# Если хотите оставить только symlink, не файлы:
public/storage

# bootstrap cache (оставляем .gitignore в каталоге)
bootstrap/cache/*

# Homestead / Valet / прочее локальное
Homestead.yaml
Homestead.json

# Временные/служебные
*.pid
*.seed
*.pid.lock
