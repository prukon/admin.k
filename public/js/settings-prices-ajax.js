document.addEventListener('DOMContentLoaded', function () {

    // User
    let teams = document.querySelectorAll('#left_bar .wrap-team');
console.log(1);
console.log(teams);
    for ( i = 0; i < teams.length; i++) {
        teams[i]

        // teams[0].id //передать

    }
});
